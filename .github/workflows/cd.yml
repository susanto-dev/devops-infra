name: Continuous deployment

on:
  workflow_dispatch:

jobs:
  build:
    name: CD
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          token: ${{ secrets.ACCESS_TOKEN }}
          ref: develop

#      - name: Checkout CI scripts
#        uses: actions/checkout@v4
#        with:
#          repository: susanto-dev/java-component
#          fetch-depth: 0
#          ref: main
#          token: ${{ secrets.ACCESS_TOKEN }}

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'zulu'
      - name: Authenticate with github package
        shell: bash
        run: |
          rm ~/.m2/settings.xml
          cat > ~/.m2/settings.xml <<EOF
          <settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0
                      http://maven.apache.org/xsd/settings-1.0.0.xsd">
          <activeProfiles>
            <activeProfile>github</activeProfile>
          </activeProfiles>
          <profiles>
           <profile>
            <id>github</id>
            <repositories>
            <repository>
            <id>github</id>
             <url>https://maven.pkg.github.com/susanto-dev/java-component</url>
             <snapshots>
              <enabled>true</enabled>
             </snapshots>
            </repository>
            </repositories>
           </profile>
          </profiles>
          <servers>
           <server>
            <id>github</id>
            <username>learnwithjj</username>
            <password>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</password>
           </server>
          </servers>
          </settings>
          EOF

      - name: Download Artifact from GitHub Packages
        env:
          GITHUB_ACTOR: ${{ secrets.GITHUB_ACTOR }}
          GITHUB_TOKEN: ${{ secrets.ACCESS_TOKEN }}
        run: |
           cat ~/.m2/settings.xml
#           mvn dependency:get -Dartifact=com.example:java-component:0.1.0-alpha.58 -DremoteRepositories=https://maven.pkg.github.com/susanto-dev/java-component -s ~/.m2/settings.xml
           curl -O -L \
           -H "Accept: application/vnd.github+json" \
           -H "Authorization: Bearer $GITHUB_TOKEN" \
           -H "X-GitHub-Api-Version: 2022-11-28" \
           https://maven.pkg.github.com/susanto-dev/java-component/com/example/java-component/0.1.0-alpha.58/java-component-0.1.0-alpha.58.jar
          ls
